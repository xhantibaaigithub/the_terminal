import 'package:flutter/material.dart';

class StreetPost extends StatelessWidget {
  const StreetPost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
